# Personal vault content — your own characters / comments / mail / settings.
data.json
character-data.json
stream-data.json
mail-data.json
blog-data.json
news-data.json
program-data.json
oracle-data.json
CLAUDE.md
AGENTS.md

# Corruption backups written by readJsonFile
*.bak

# Build artifacts — shipped via GitHub releases, not the repo.
# Leading slashes anchor these to the repo root so they do NOT also match src/.
/main.js
/styles.css

# Node
node_modules/

# Misc
tools/
